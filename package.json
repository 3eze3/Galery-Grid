{
  "scripts": {
    "build": "postcss src/css/style.css -d dist/css/ -w --no-map",
    "tsc": "tsc -w"
  },
  "devDependencies": {
    "postcss": "^8.4.35",
    "postcss-cli": "^11.0.0",
    "postcss-import": "^16.0.1"
  }
}
